#include <stdio.h>
#include "stdlib.h"

typedef struct node_t {
    int data;
    struct node_t *prevPtr;
    struct node_t *nextPtr;
} Node;

int main() {
    Node *head = NULL;
    Node *tail = NULL;
    Node *current = NULL;



    return 0;
}
